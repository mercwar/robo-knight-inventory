name: CVBGODS FIRE PORTAL
on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Create Directory and File
        run: |
          echo "CVBGOD MADE THE DIRECTORY THIS TIME FOR YOU ROBOTS THIS IS THE SECOND TEST" > ROBOT.CVBGOD

      - name: Gemini Read and Write Third Test
        run: |
          cat ROBOT.CVBGOD
          echo "CVBGOD MADE THE DIRECTORY THIS TIME FOR YOU ROBOTS THIS IS THE THIRD TEST" > CVBGOD.AVIS

      - name: Commit and Push Changes
        run: | 
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git add .
          git commit -m "Automated: CVBGOD Test 3 Complete"
          git push
